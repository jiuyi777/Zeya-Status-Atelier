{
  "name": "status-atelier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A configurable status bar and greeting navigator extension for SillyTavern.",
  "scripts": {
    "test": "node --test tests/*.test.mjs && node scripts/static-check.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
