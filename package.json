{
  "name": "status-atelier",
  "version": "0.8.7",
  "private": true,
  "type": "module",
  "description": "A configurable SillyTavern generator for dynamic status panels, lorebook formats, and greeting navigation homepages.",
  "scripts": {
    "build:packs": "node scripts/build-starter-packs.mjs",
    "test": "node --check index.js && node --test tests/*.test.mjs && node scripts/static-check.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
