<jiuyi_status_type_09_rules>
每次正文结束后，必须追加一个 <jiuyi_status_type_09> 状态区块。
区块中的所有值都必须根据当前剧情动态生成；模板中的双花括号只是填写说明，回复时不得原样保留。
方括号内严格使用英文竖线分隔。值中不得出现英文竖线、方括号、尖括号或 Markdown 加粗。
不要把状态区块放进 Markdown 代码块，不要输出 HTML，不要遗漏记录，也不要附加同类的第二套状态格式。

切换页对应关系：
- View1 对应“当前终端数据屏”：填写当前剧情中最重要的终端数据屏对象

每个切换页的动态字段顺序：
- 第1项“主节点”：根据当前剧情简短填写主节点
- 第2项“连接状态”：根据当前剧情简短填写连接状态
- 第3项“动态值 A”：根据当前剧情填写动态值 A，只写0到100之间的整数
- 第4项“动态值 B”：根据当前剧情填写动态值 B，只写0到100之间的整数
- 第5项“主日志”：根据当前剧情完整填写主日志
- 第6项“强调信息”：根据当前剧情完整填写强调信息

严格输出模板：
<jiuyi_status_type_09>
[View1|{{当前终端数据屏·主节点：根据当前剧情简短填写主节点}}|{{当前终端数据屏·连接状态：根据当前剧情简短填写连接状态}}|{{当前终端数据屏·动态值 A：根据当前剧情填写动态值 A，只写0到100之间的整数}}|{{当前终端数据屏·动态值 B：根据当前剧情填写动态值 B，只写0到100之间的整数}}|{{当前终端数据屏·主日志：根据当前剧情完整填写主日志}}|{{当前终端数据屏·强调信息：根据当前剧情完整填写强调信息}}]
</jiuyi_status_type_09>
</jiuyi_status_type_09_rules>
