<jiuyi_status_type_09_rules>
每次正文结束后，必须追加一个 <jiuyi_status_type_09> 状态区块。
区块中的所有值都必须根据当前剧情动态生成；模板中的双花括号只是填写说明，回复时不得原样保留。
方括号内严格使用英文竖线分隔。值中不得出现英文竖线、方括号、尖括号或 Markdown 加粗。
不要把状态区块放进 Markdown 代码块，不要输出 HTML，不要遗漏记录，也不要附加同类的第二套状态格式。

切换页对应关系：
- View1 对应“当前全息终端”：填写当前剧情中最重要的全息终端对象

每个切换页的动态字段顺序：
- 第1项“会话”：根据当前剧情简短填写会话
- 第2项“网络”：根据当前剧情简短填写网络
- 第3项“CPU”：根据当前剧情填写CPU，只写0到100之间的整数
- 第4项“内存”：根据当前剧情填写内存，只写0到100之间的整数
- 第5项“命令日志”：根据当前剧情完整填写命令日志
- 第6项“告警”：根据当前剧情完整填写告警

严格输出模板：
<jiuyi_status_type_09>
[View1|{{当前全息终端·会话：根据当前剧情简短填写会话}}|{{当前全息终端·网络：根据当前剧情简短填写网络}}|{{当前全息终端·CPU：根据当前剧情填写CPU，只写0到100之间的整数}}|{{当前全息终端·内存：根据当前剧情填写内存，只写0到100之间的整数}}|{{当前全息终端·命令日志：根据当前剧情完整填写命令日志}}|{{当前全息终端·告警：根据当前剧情完整填写告警}}]
</jiuyi_status_type_09>
</jiuyi_status_type_09_rules>
