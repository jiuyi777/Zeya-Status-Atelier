<jiuyi_status_type_05_rules>
每次正文结束后，必须追加一个 <jiuyi_status_type_05> 状态区块。
区块中的所有值都必须根据当前剧情动态生成；模板中的双花括号只是填写说明，回复时不得原样保留。
方括号内严格使用英文竖线分隔。值中不得出现英文竖线、方括号、尖括号或 Markdown 加粗。
不要把状态区块放进 Markdown 代码块，不要输出 HTML，不要遗漏记录，也不要附加同类的第二套状态格式。

切换页对应关系：
- View1 对应“当前大号数字牌”：填写当前剧情中最重要的大号数字牌对象

每个切换页的动态字段顺序：
- 第1项“对象”：根据当前剧情简短填写对象
- 第2项“当前状态”：根据当前剧情简短填写当前状态
- 第3项“核心数值”：根据当前剧情简短填写核心数值
- 第4项“动态数值”：根据当前剧情填写动态数值，只写0到100之间的整数
- 第5项“辅助信息”：根据当前剧情简短填写辅助信息
- 第6项“正文备注”：根据当前剧情完整填写正文备注

严格输出模板：
<jiuyi_status_type_05>
[View1|{{当前大号数字牌·对象：根据当前剧情简短填写对象}}|{{当前大号数字牌·当前状态：根据当前剧情简短填写当前状态}}|{{当前大号数字牌·核心数值：根据当前剧情简短填写核心数值}}|{{当前大号数字牌·动态数值：根据当前剧情填写动态数值，只写0到100之间的整数}}|{{当前大号数字牌·辅助信息：根据当前剧情简短填写辅助信息}}|{{当前大号数字牌·正文备注：根据当前剧情完整填写正文备注}}]
</jiuyi_status_type_05>
</jiuyi_status_type_05_rules>
