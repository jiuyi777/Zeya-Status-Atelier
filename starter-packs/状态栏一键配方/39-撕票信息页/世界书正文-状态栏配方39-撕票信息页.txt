<jiuyi_status_type_19_rules>
每次正文结束后，必须追加一个 <jiuyi_status_type_19> 状态区块。
区块中的所有值都必须根据当前剧情动态生成；模板中的双花括号只是填写说明，回复时不得原样保留。
方括号内严格使用英文竖线分隔。值中不得出现英文竖线、方括号、尖括号或 Markdown 加粗。
不要把状态区块放进 Markdown 代码块，不要输出 HTML，不要遗漏记录，也不要附加同类的第二套状态格式。

切换页对应关系：
- View1 对应“当前撕票信息页”：填写当前剧情中最重要的撕票信息页对象

每个切换页的动态字段顺序：
- 第1项“主标题”：根据当前剧情简短填写主标题
- 第2项“分类”：根据当前剧情简短填写分类
- 第3项“动态数值”：根据当前剧情填写动态数值，只写0到100之间的整数
- 第4项“日期时间”：根据当前剧情简短填写日期时间
- 第5项“位置 / 对象”：根据当前剧情简短填写位置 / 对象
- 第6项“编号”：根据当前剧情简短填写编号

严格输出模板：
<jiuyi_status_type_19>
[View1|{{当前撕票信息页·主标题：根据当前剧情简短填写主标题}}|{{当前撕票信息页·分类：根据当前剧情简短填写分类}}|{{当前撕票信息页·动态数值：根据当前剧情填写动态数值，只写0到100之间的整数}}|{{当前撕票信息页·日期时间：根据当前剧情简短填写日期时间}}|{{当前撕票信息页·位置 / 对象：根据当前剧情简短填写位置 / 对象}}|{{当前撕票信息页·编号：根据当前剧情简短填写编号}}]
</jiuyi_status_type_19>
</jiuyi_status_type_19_rules>
