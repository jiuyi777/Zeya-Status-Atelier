# 正则状态工坊优化管理清单

> 基线日期：2026-08-11。本文只管理 `C:\sta\status-atelier` 当前插件，不代表 SillyTavern、TauriTavern 或用户设备已经验收。

## 当前基线

- 分支：`main`，开始接手时与 `origin/main` 对齐；上游最新提交为 `0d9dd7d fix TavernHelper iframe detection`。
- 接手时版本：`0.8.10`；当前待发布工作版本：`0.9.0`。
- `AGENTS.md`、复盘规范与本清单是用户要求保留并在后续插件任务中必读的项目文档，本次与对应实现一起提交。
- 用户已在当前任务明确要求全部测试通过后上传 GitHub。
- 本地 `C:\sta\SillyTavern` 没有安装 TavernHelper，当前也没有 TauriTavern 设备环境。

## 问题清单

| 编号 | 链路 | 状态 | 当前结论与验收条件 |
| --- | --- | --- | --- |
| SA-001 | 世界书切换 | 已修复，待运行验收 | 主页优先使用官方现行 `updateWorldbookWith`，保留 `setLorebookEntries` 与 Slash 回退；需在装有 TavernHelper 的 ST/TT 中验证真实 UID 开关。 |
| SA-002 | 内嵌世界书 | 已修复，待运行验收 | 卡内 `character_book` 可提供线路标签，但未导入为世界书文件时不再显示成可切换 UID；空绑定线路不会关闭其他路线，同名已绑定世界书仍可建立真实绑定。 |
| SA-003 | 移动端 UI | 已修复，待设备验收 | 主页制作弹窗、UID 选择框增加安全区，窄屏色卡按钮不小于 44px；需在 TT 竖屏、横屏和软键盘弹出状态下验收。 |
| SA-004 | 主页 iframe | 静态与生成物契约已覆盖 | 成品必须是 fenced HTML，含完整 `<body>...</body>`，`placement` 为 `[1, 2]`；需在 TavernHelper 渲染器中确认按钮真实执行。 |
| SA-005 | 额外问候语跳转 | 静态模拟已覆盖 | 优先 `setChatMessages([{ message_id: 0, swipe_id }])`，旧环境回退原生 swipe；需分别验证主页在第 0 页、额外问候语 #1/#N 及长聊天场景。 |
| SA-006 | 生成与解析 | 静态与真实 API 验收通过 | JSON Schema、网关包裹、思维链标签、编号文本回退和 502/524 区分均有测试；隔离酒馆实例已用 `gg`、`gemini-3.1-pro-preview` 与已保存 Key 完成真实请求，1 页 6 字段全部解析。 |
| SA-007 | 用户参考正则 | 待补证据 | 当前仓库未找到复盘所述 `regex-开场白.json`；取得真实参考后必须逐字段比较 `findRegex`、`placement`、`markdownOnly`、`runOnEdit`、完整 HTML 外壳与 API 调用。 |
| SA-008 | 四套主页模板 | 生成物通过，视觉待验收 | 四套成品可重复构建且 JSON 可解析；版式、折叠、长标题、多行推荐和触摸体验需逐套实机检查。 |
| SA-009 | 五十套状态栏 | 生成物与浏览器渲染通过，设备待验收 | 保留 50 套可编辑主题，并新增 9 种独立组件结构、12 套可组装色卡以及头像、封面和音频来源；实际正则渲染器已在移动端与宽屏浏览器夹具中逐项显示，TT 设备仍待验收。 |
| SA-010 | 旧成品更新 | 发布前待说明 | 已导出的旧正则不会自动变化；发布或交付时必须提示用户重新生成、覆盖或重新导入。 |

## 发布门槛

### 1. 静态代码检查

- [x] `node --check index.js`
- [x] Node 回归测试通过（当前 58 项）
- [x] `scripts/static-check.mjs` 通过
- [x] 作者署名为“九一”，`package.json` 与 `manifest.json` 版本一致
- [x] 最终修改范围复核，规范文档与实现均属于用户要求的插件工作范围

### 2. 最终生成物检查

- [x] `npm run build:packs` 可重复生成且接手基线无漂移
- [x] 四套主页 JSON 可解析，含 fenced HTML、完整 body、`placement: [1, 2]`
- [x] 五十套状态栏正则与世界书 JSON 可解析
- [x] 主页成品包含现行 Worldbook API 与旧接口回退
- [x] 版本 `0.9.0` 再次确认 Git 差异、成品头尾和无旧捕获占位残留

### 3. 本地浏览器或模拟环境

- [ ] 主页四套模板逐套点击、折叠和长内容检查
- [x] 状态栏 9 种组件结构使用实际正则渲染器完成移动端与宽屏浏览器检查
- [ ] 移动端窄屏、横屏、软键盘和安全区检查
- [ ] 只可表述为浏览器/模拟结果，不得写成 ST/TT 或设备通过

### 4. SillyTavern / TavernHelper 运行

- [ ] TavernHelper 识别代码块并创建 iframe
- [ ] `setChatMessages` 真实切换额外问候语
- [ ] `updateWorldbookWith` 只修改目标 UID 的 `enabled`，其他条目保持不变
- [ ] 世界书修改失败时问候语仍可切换并显示明确反馈
- [x] 隔离 SillyTavern 实例通过保存的 `gg` Key 发起 Gemini 请求，返回内容通过状态栏完整性解析

### 5. TauriTavern 与用户设备

- [ ] TT 竖屏、横屏、返回聊天、长聊天虚拟化场景
- [ ] 触摸按钮、滚动、底部安全区和软键盘遮挡
- [ ] 用户设备截图或录像验收

### 6. 发布动作

- [x] 用户在当前任务中明确授权提交
- [x] 用户在当前任务中明确授权推送或发布
- [x] 提交前再次检查 manifest、工作树与远端仓库身份
- [ ] 未完成的 ST/TT/设备验收在发布说明中明确列出

## 直接接口依据

- TavernHelper 渲染器：代码块同时含 `<body>` 与 `</body>` 才会被识别为 iframe 前端。
- TavernHelper 楼层消息：`setChatMessages([{ message_id: 0, swipe_id }])` 是官方切换开局示例。
- TavernHelper 世界书：现行接口为 `updateWorldbookWith`，条目启用字段为 `enabled`；旧 Lorebook 接口仅保留兼容回退。
