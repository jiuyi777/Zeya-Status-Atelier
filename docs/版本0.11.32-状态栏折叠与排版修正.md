# v0.11.32 状态栏折叠与排版修正

01—15 的折叠入口使用各自配色的纸签与标签，保留月下蝶影拼贴，人物卷宗收起后使用档案纸条。竖排入口只显示“收起”两字并禁止换列，避免文字挤成两列。

修正 05—09 手机布局收起后仍保留展开高度的问题，调整摘要布局及 05 头像留白。调整 01 头像、说明与印章位置，避免遮挡；缩小 02 行程卡片。生成页面隐藏滚动条外观。

修改进入共享生成逻辑，未增加嵌入图片，沿用轻量导出处理。更新插件后需重新生成并覆盖对应状态栏；旧导出角色卡不会自动改变。

验证：15 款本地预览已重新生成，键盘展开与收起检查通过；05 最新两字纸签已截图核对。自动测试及静态检查通过。尚未在接收者 ST/TT 或真实手机环境验收。
