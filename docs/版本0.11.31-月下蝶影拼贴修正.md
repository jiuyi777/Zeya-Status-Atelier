# v0.11.31 月下蝶影拼贴修正

月下蝶影在手机宽度保留原拼贴构图，替换旧版长列表。普通二三十至四十字内容按两行居中排布，保留格内留白，不添加格内滚动。收起后显示照片、纸张标题与原装饰素材组成的拼贴栏，展开时使用纸质书签入口。

折叠栏在运行时复用已有图片，不增加一份嵌入素材；继续使用 v0.11.30 的压缩导出入口。修复收起状态下不应测量隐藏文字的问题。

本地浏览器已验证展开、收起及示例文字。未在接收者 ST/TT 设备验收。更新插件后需重新生成并覆盖该状态栏，旧导出卡不会自动改变。本次只调整月下蝶影；其他模板保留现状供逐款审阅。
