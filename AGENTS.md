# Status Atelier 仓库工作约束

在本仓库开始任何设计、诊断、修改、测试或发布工作前，必须完整阅读：

- `docs/插件开发复盘与必读规范.md`

该文件是本仓库的强制工作规范。不得跳过，也不得以旧任务记忆、既有测试结果或先前结论代替本次阅读。

## 范围限制

- 默认只读取 `C:\sta\status-atelier` 中与当前问题直接相关的文件。
- 需要核对 SillyTavern、TauriTavern 或 TavernHelper 时，只定位并读取当前功能调用涉及的具体模块、接口、文档页面或用户提供的参考文件。
- 禁止为了“熟悉项目”递归扫描整个 `C:\sta`、整个 SillyTavern 源码、全部扩展目录或所有用户文件。
- 只有在相关局部资料无法解释问题时，才可以扩大读取范围；扩大前必须说明缺少什么证据、准备读取哪里。
